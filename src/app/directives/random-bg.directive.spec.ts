import { RandomBgDirective } from './random-bg.directive';

describe('RandomBgDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomBgDirective();
    expect(directive).toBeTruthy();
  });
});
